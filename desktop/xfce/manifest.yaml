name: ubuntu-xfce
layer: desktop
description: XFCE desktop, VNC + RDP, derived from ubuntu-base-rdp.
version: "1.0.3"
from: ubuntu-base-rdp

variants:
  - name: ubuntu-xfce
    smoke:
      vnc: true
      rdp: true
      audio: true
      env:
        WAAS_RDP_ENABLED: "1"
